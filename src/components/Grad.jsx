// TODO: Place a gradient block randomly from -500px to vw+500px width in every 1000px height scrolls

const Grad = (props) => {
	return (
		<div
			className="grad"
			style={props.style}
		></div>
	);
};

export default Grad;
